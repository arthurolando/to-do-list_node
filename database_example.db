CREATE DATABASE IF NOT EXIST todolist;
USE todolist;

CREATE TABLE tasks(  
    id INTEGER PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(45) NOT NULL,
    status VARCHAR(45) NOT NULL,
    created_at VARCHAR(45) NOT NULL
);